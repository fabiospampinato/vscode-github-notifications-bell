
/* MAIN */

type Options = {
  refreshInterval: number,
  alignment: string,
  icon: string,
  color: string,
  hideIfNone: boolean,
  showNumberOfNotifications: boolean,
  protocol: string,
  domain: string
};

/* EXPORT */

export type {Options};
