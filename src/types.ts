
/* MAIN */

type Options = {
  refreshInterval: number,
  token?: string,
  alignment: string,
  icon: string,
  color: string,
  hideIfNone: boolean,
  showNumberOfNotifications: boolean,
  protocol: string,
  domain: string
};

/* EXPORT */

export type {Options};
